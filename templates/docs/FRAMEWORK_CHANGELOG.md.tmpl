# Framework changelog

> ⚠️ **Stub — Phase 1.**

Append-only log of every change to this project's local framework.
Newest at top. Each entry is the output of a retrospective per
`docs/RETROSPECTIVE_PROTOCOL.md`.

This file IS the history of *your* framework. Reading top-to-bottom
tells you why every local rule exists and what triggered it.

## Open retrospective items

(populated as retrospectives defer fixes)

---

## YYYY-MM-DD — Framework v0.0.1 (initialization)

### Added
- Cadence framework scaffolded via `/cadence-init`
- 7 default ADRs seeded as starting points (review and prune for your
  project)
- Boundary rules in `.cadence/cadence.yaml` from the Cadence default
  for the {{stack}} stack
