# Team launch template

> ⚠️ **Stub — Phase 1.** Full template arrives in Phase 1.

Fill in `{{...}}` placeholders and paste into the Claude Code session.
Or invoke `/cadence-launch <feature>` to do it interactively.

```
Before doing anything else, read these in order and confirm:
1. CLAUDE.md
2. docs/PATTERNS.md
3. docs/DEFINITION_OF_DONE.md
4. docs/ROLE_SPECS.md
5. docs/TEAM_PROTOCOL.md
6. The ADRs in docs/ADR/ that apply to this work

Then create an agent team to execute the feature below. Spawn the
four Cadence teammates with the file ownership defined in
docs/ROLE_SPECS.md.

────────────────────────────────────────────────────────────────────
FEATURE: {{one-line description}}

REQUIREMENTS:
{{bulleted list of user-facing behavior}}

ACCEPTANCE CRITERIA (beyond DoD):
{{any feature-specific criteria, or "none beyond DoD"}}

OUT OF SCOPE:
{{explicit list, or "none"}}
────────────────────────────────────────────────────────────────────

Teammates: cadence-data-engineer, cadence-feature-engineer,
cadence-tester, cadence-reviewer.

Done is defined by docs/DEFINITION_OF_DONE.md.

Before declaring the team done, the Reviewer runs the retrospective
per docs/RETROSPECTIVE_PROTOCOL.md.
```
